import styled from "styled-components";
import { colors } from "../../utils/colors";

export const ContainerTasksStyled = styled.section`
    background-color: ${colors.color_transparent};
    width: 60vw;
    min-height: 40vh;
    padding: 2rem;
    box-shadow: 2px 2px 5px 0px ${colors.transparent_dark};
`;
